---
title: Changelog
---

<Update label="September 2026" description="Unreleased" tags={["Improvement"]}>
  - **Send feedback from Settings.** A compact Feedback button at the bottom of the sidebar opens an email draft to Max in your mail app, with the subject “Focuh feedback”.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Improvement"]}>
  - **Switch pages with ⌘↑ and ⌘↓.** In the Mac app, move to the previous or next page in sidebar order, skipping Journal when it is disabled. In Settings, the same shortcuts move between its visible sections. They stop at either end and leave typing, menus and dialogs alone.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Improvement"]}>
  - **Two full-height panels.** The Mac window controls and subtle back/forward chevrons now sit within the navigation surface, without a separate top-bar divider. Main content and the optional calendar stay solid.
  - **Hide navigation completely.** Collapsing removes the entire sidebar and its history arrows. The reopen icon floats beside the traffic lights; there is no leftover icon rail. You can reopen navigation even in a narrow window.
  - **One transparency setting.** Appearance controls only the sidebar, defaulting to 20% transparency on a new device. Existing sidebar choices are preserved; the old main-panel transparency setting no longer applies.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Bug Fix"]}>
  - **A consistent divider below the top bar.** The Mac app's top-bar separator now stays visible at every transparency setting, matching the sidebar divider without changing the bar's height or control alignment.
  - **Quieter dividers.** Decorative separators are softer in light mode and much dimmer in dark mode. Buttons, input borders and keyboard focus rings keep their existing contrast.
</Update>

<Update label="September 2026" description="Unreleased" tags={["New Feature"]}>
  - **Connect a Google account read-only.** Preferences → Calendar has a new Connect read-only option that only asks Google for permission to see your calendars, the smallest request a managed work account can approve. Its events show up like any other, but Focuh never writes to it: tasks can’t be added to it, new events can’t be created on its calendars, and moving, renaming or deleting one of its events is refused with a clear message. A Read-only tag marks it in the list. The same rule now covers accounts that unticked "edit events" on Google’s consent screen.
  - **The Mac app can’t get stuck on a Google page.** Connecting a Google account from the Mac app now opens your browser for the consent step and picks the new account up when you come back. If the app window is ever sent to a page outside Focuh, it returns to where you were and opens that page in your browser instead.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Improvement"]}>
  - **Finished tasks get out of the way.** In Today and Week, completed tasks stay in place for three seconds, then move to the bottom of Nice to have. Reopening a task returns it to its original priority section.
  - **Nice to have remembers.** Its collapsed or expanded state survives navigation and reloads, including independently for each day in Week. Completing a task won't expand it.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Improvement", "Bug Fix"]}>
  - **Two transparency controls.** Appearance adds a separate 0–100 slider for the main panel. The sidebar slider also controls the top bar. Only backgrounds fade; cards and text stay readable, and native macOS blur stays on even at 100.
  - **Quieter Settings navigation.** Removed the full-window fade and slide that briefly made translucent backgrounds solid. Both sliders now cross Solid without restarting native blur. The next desktop build also keeps the native material in place during theme updates; v0.8.0 retains a safe solid fallback while its theme updates.
</Update>

<Update label="September 2026" description="Unreleased" tags={["Improvement"]}>
  - **Dial in your sidebar.** Appearance now has a 0–100 transparency slider instead of three presets. Higher values reveal more of the native macOS background blur without fading labels or main content; 100 removes the extra color tint, not the blur. Your choice stays on this Mac, and accessibility settings still temporarily force Solid. Works with Focuh for Mac v0.8.0 or newer.
</Update>

<Update label="September 2026" description="v2.20.1" tags={["Bug Fix"]}>
  - **Resize, don't move.** Dragging the top or bottom edge of a short calendar block now stretches it, instead of picking the whole block up and moving it. This fix mainly affects the Mac app.
</Update>

<Update label="September 2026" description="v2.20.0" tags={["Improvement"]}>
  - **Buttons that look like buttons in every color.** Main actions are now a deeper accent fill with white text, so Generate key, Complete, Save and friends read clearly under Sage, Ink, Orchid, your custom color and in dark mode — no more washed-out pill with black text.
  - **Hover, press and focus feel the same everywhere.** Every button darkens on hover, presses down slightly, and shows a keyboard focus ring in your accent color that sits correctly on dark backgrounds too.
  - **Delete actions are red.** Buttons that erase something now have their own red style, so a delete confirmation looks like one. Return still confirms it, as before.
  - Buttons that are working (generating a key, exporting your data) keep their label and show a spinner instead of going grey.
</Update>

<Update label="September 2026" description="v0.8.0" tags={["New Feature"]}>
  - **A softer Mac sidebar.** Preferences → Appearance adds Solid, Subtle, and More transparent backgrounds, tinted with your chosen color. Your choice stays on this Mac; content remains opaque, and macOS accessibility settings temporarily keep the sidebar solid. Solid remains the default. Requires Focuh for Mac v0.8.0 or newer.
</Update>

<Update label="September 2026" description="v2.19.2" tags={["Improvement"]}>
  - **More room for subtasks.** In short focus windows, the timer, title, and progress bar sit a little higher. The list fades softly at the bottom, while the last row and editor can still scroll fully into view.
</Update>

<Update label="September 2026" description="v2.19.1" tags={["Bug Fix"]}>
  - **Keep your focus controls in view.** Long subtask lists now scroll on their own while the timer, task title, progress bar, and Complete/Pause buttons stay in place. Short windows use tighter spacing, and every subtask and the Add subtask row remain reachable.
</Update>

<Update label="September 2026" description="v2.19.0" tags={["Bug Fix"]}>
  - **A more recent community.** The dropdown now lists members active in the last 12 hours, including across midnight. Today's focus totals and the online count are unchanged.
  - **Finishing a task ends its timer everywhere.** Tick the task you are working on from another device, Raycast, an AI assistant or the board and the Focuh session ends on every device, with the time logged. The Focuh screen no longer re-opens on a task you already finished, and if it does show one, Complete closes it. Finished tasks no longer offer a Focus button.
</Update>

<Update label="September 2026" description="v2.18.1" tags={["Bug Fix"]}>
  - **A quieter community dropdown.** Removed streak badges and device icons; names, ranks, focus time, and status stay visible.
  - **Readable goals in Week.** Linked goal names on task cards now adapt to light and dark themes instead of disappearing against dark backgrounds.
  - **Move the whole selection.** Choosing “Move to tomorrow” from a selected task’s dropdown now moves all selected tasks in Today and Week, with one undo for the group.
  - **One calendar event, one block.** Events shared across connected Google accounts now appear once. Separate events and recurring dates stay separate, and Focuh prefers a writable copy when available.
  - **Progress follows the timer.** The focus progress bar now tracks elapsed time against your estimate and stays still while paused.
</Update>

<Update label="September 2026" description="v2.18.0" tags={["New Feature"]}>
  - **Pick a color, not a hex code.** Preferences → Appearance now shows nine named colors — Sage, Tide, Glacier, Ink, Dusk, Orchid, Rose, Ember and Graphite — and one tap re-tints the whole app in light and dark. Want your own? Choose Custom and drag the color strip; no hex codes, no sliders to decode. Your choice follows you across devices.
  - Fixed: a custom color could flash back to green for a moment on launch, and opening the old picker without changing anything used to save a duplicate green.
</Update>

<Update label="September 2026" description="v0.7.3" tags={["Bug Fix"]}>
  - **Remove the schedule, keep the task.** Backspace, Delete, and the trash button on a scheduled task now remove only its calendar block and synced Google Calendar event. The task and its subtasks stay in your list.
  - **Undo sticks around.** ⌘Z restores calendar changes even after the notification disappears. The last ten actions stay available during your signed-in session, and dragging a task back off the calendar can be undone too.
  - **Undo works from the Mac menu.** The desktop app's Edit → Undo and ⌘Z now reach app actions while keeping normal undo inside text fields and the journal.
</Update>

<Update label="September 2026" description="v2.17.0" tags={["Bug Fix"]}>
  - **Return confirms a dialog.** In every Focuh dialog, pressing Return now triggers the main button, so a delete confirmation deletes instead of closing. Escape still cancels.
</Update>

<Update label="September 2026" description="v2.16.0" tags={["New Feature"]}>
  - **Rename from the calendar.** Double-click an event or a scheduled task on the Today calendar to rename it in place. Google Calendar events and synced tasks update in Google too.
</Update>

<Update label="September 2026" description="v2.15.0" tags={["Improvement"]}>
  - **Leaderboard settings, one hover away.** The focuh-munnity panel now has a small settings button in its corner that opens your leaderboard preferences.
</Update>

<Update label="September 2026" description="v2.14.0" tags={["Improvement"]}>
  - **The timer rolls.** Digits on the Focuh screen's timer now slide to the next value instead of blinking, and only the digit that changed moves. Turn on Reduce Motion in System Settings to keep the plain swap.
</Update>

<Update label="September 2026" description="v2.13.0" tags={["Improvement"]}>
  - **One click opens everything.** Clicking the Focuh item in the menu bar now brings the app window forward and opens the menu at the same time. Before, it did one or the other depending on where in the title you clicked.
</Update>

<Update label="September 2026" description="v2.12.0" tags={["Improvement"]}>
  - **A readable menu bar.** The Mac menu bar now shows up to 20 characters of the task you're working on instead of six.
  - **Minutes, not seconds.** The menu bar countdown reads `1h 12m` and updates once a minute. Turn Countdown off in Preferences → Menu bar to see `1h 12m left` instead.
  - **A tidier progress ring.** The ring next to the timer is smaller and a little heavier so it reads at a glance.
</Update>

<Update label="September 2026" description="v2.11.0" tags={["New Feature", "Bug Fix"]}>
  - **The Mac app no longer hangs without internet.** Opening Focuh offline used to leave it stuck on the loading screen forever. It now explains what's wrong, reconnects on its own, and picks up where you left off.
  - **You can see when you're offline.** A quiet indicator appears in the sidebar when Focuh can't reach the server. Your changes are kept and sync automatically once you're back.
  - **No more silently lost minutes.** If you stay offline long enough for a running session to be at risk, Focuh now warns you which minutes won't be counted instead of dropping them without a word.
  - **Blocking keeps working offline.** Unchanged, but now stated plainly on the offline screen.
  - **Rows stop jumping when you hover a link.** The remove buttons on attached links now slide in over the end of the task title instead of pushing it around and shifting the time chip.
  - **Completed goals can be deleted.** Hover a finished goal and use the trash icon. Undo is available for a few seconds after.
  - **Resizing a calendar block no longer moves it.** In the Mac app, grabbing the top or bottom edge of a scheduled task or event used to start a drag instead of changing its length. Edge-resizing now works the same as on the web.
  - **Finished goals tidy themselves.** A completed goal stays listed under its section for a week, then leaves the Goals page on its own.
</Update>

<Update label="September 2026" description="v2.10.0" tags={["Improvement", "Bug Fix"]}>
  - **Calendar changes stay in sync.** Moving, resizing, or deleting a scheduled task now updates its Google Calendar copy from the server, including changes made through Raycast or an AI assistant.
  - **Focus days follow your timezone.** Today’s minutes, weekly totals, streaks, and community rankings now agree with the timezone saved by your device.
  - **Faster, steadier task moves.** Dragging tasks between days, Loops, and priority sections updates immediately and rolls back cleanly if the save fails.
  - **MCP 3.0.** Focuh’s MCP server now uses short-lived, scoped sessions and supports current Claude, ChatGPT, Cursor, and other MCP clients. Pinned 2.x installs should upgrade to `@focuh/mcp-server@3`.
  - **A less boring blocked screen.** Blocked sites now occasionally greet you with a focus meme.
</Update>

<Update label="September 2026" description="v2.9.1" tags={["Bug Fix"]}>
  - **“Always” now means always.** The Mac app now enforces around-the-clock block rules created by the latest interface, even when no focus session is running.
  - **One rule per website.** Pasted URLs such as `x.com/home` are reduced to their domain and merged with duplicates, so an older entry can no longer shadow its limits or schedule.
</Update>

<Update label="September 2026" description="v2.9.0" tags={["New Feature"]}>
  - **Daily limits.** Give any site or app a number of minutes per day — 30 minutes on x.com, say. Time counts while it's in front of you, you get a heads-up at 5 and 1 minutes left, and when it's gone the site is blocked until midnight. No override.
  - **Blocking hours.** Set one schedule — Mon–Fri, 9 to 5 by default — and switch any entry onto it. Those are blocked during those hours whether or not a session is running.
  - **Rules per entry.** Select an entry in Preferences → Blocking to combine *during focus*, a daily limit and the schedule however you like. Your existing block list keeps working exactly as before.
  - **Open at login.** Limits only work while Focuh is running, so Focuh can now start with your Mac (Preferences → General → Startup). Closing the window now also hides the Dock icon — Focuh lives in the menu bar until you click it.
  - **The menu bar shows what's left.** While you're on a limited site or app, the menu bar reads its remaining minutes; a running focus session still takes priority.
  - **Quitting asks first.** Quitting Focuh lists the limits that would stop being enforced before it exits.
</Update>

<Update label="September 2026" description="v2.8.0" tags={["New Feature"]}>
  - **Start focus from Raycast.** A Focuh extension for Raycast: **Search Tasks** lists today's plan and starts a session with ↩, **Start Focus** takes a task name (or picks your top task), and **Pause or Resume** / **Stop** work from anywhere. Sign in once with your Focuh account; the Mac app picks the session up on its own — menu bar timer, site and app blocking, the start bell — and comes to the front on Today. Local install for now: `cd raycast && npm install && npm run dev`.
  - **Focus session API.** The API-key endpoints gained `GET /api/mcp/focus` and `POST /api/mcp/focus/{start,stop,pause,resume}`, so any integration with a Focuh key can drive the timer.
</Update>

<Update label="September 2026" description="v2.7.0" tags={["New Feature"]}>
  - **Goals now have a clock.** "This week" and "This month" show how long is left in the period — "4d left", then "Last day". A goal that outlives its week or month doesn't vanish or tick itself off: it goes into overtime, with "+38d" beside it counting the days, the same way the focus timer counts past its estimate. Finish it, drop it, or pick **Carry over** from its ⋯ menu to bring it into the current week or month. Your week goal also sits in the Week and Today headers with its clock — click it to jump to Goals.
</Update>

<Update label="September 2026" description="v2.6.1" tags={["Bug Fix"]}>
  - **Focuh stays in your menu bar.** Closing the window used to quit the whole app — the timer icon just vanished. Now the window closes, and Focuh keeps running in the menu bar; click the menu bar icon or the dock icon to bring it back. Quit still works from ⌘Q or the menu bar icon's right-click menu.
  - **Connect Google Calendar, however you signed in.** If you joined Focuh without Google, the Calendar settings told you to "sign in with Google" — with no button to do it. There's a real Connect button now, and it can link any Google account, including one different from your login. The calendar panel also points you there instead of sitting empty.
</Update>

<Update label="September 2026" description="v2.6.0" tags={["New Feature", "Improvement"]}>
  - **A real title bar.** The Mac app now has a full-width top bar, Linear-style: the traffic lights sit centered in it, the sidebar collapse toggle lives right beside them, and the rest is yours to grab and drag.
  - **Back and forward, like a browser.** New arrows in the title bar — plus ⌘[ and ⌘] — walk your navigation history. Escape from Settings takes you back to where you actually came from.
  - **Sharper traffic lights.** The window buttons stopped misfiring their hover glyphs, stay clickable across their whole strip, and no longer glitch when leaving fullscreen.
</Update>

<Update label="August 2026" description="v2.5.1" tags={["Bug Fix"]}>
  - **Grab the window anywhere along the top.** On the Mac app, the top strip now drags the window from every view — the title, the calendar header, the empty gaps beside them — not just a few lucky spots. And no more text cursor when you meant to move the window.
  - **Clicks stay clicks.** Buttons in the top strip keep working, a plain click never nudges the window, and double-clicking an empty spot zooms it — just like a native title bar.
</Update>

<Update label="August 2026" description="v2.5.0" tags={["New Feature", "Improvement"]}>
  - **Board view for Today.** Flip Today from a list into a four-column status board — Todo, In progress, Waiting, Done — crossed with your Critical / Important / Nice to have lanes. Drag cards between columns; drop one into Waiting and note who it's blocked on.
  - **Drag the real thing.** Picking up a card lifts the card itself, held where you grabbed it — no more tiny stand-in chip.
  - **Attachments within reach.** A card's links (GitHub, Slack, Framer, anything) ride the hover bar next to the timer controls, so they stay clickable while you hover.
  - **A quieter board.** Simpler lane headings, calmer empty cells, roomier cards — and the In progress dot breathes while the day is running.
  - **A fresh coat everywhere.** The whole app moved to a new styling engine with a consolidated color system, sharpening dark mode and paving the way for accent themes.
</Update>

<Update label="August 2026" description="v2.4.0" tags={["New Feature"]}>
  - **Waiting loops.** A fourth bucket in Loops for anything you can't act on yet — blocked on someone else, waiting on a reply, not this month. Park it and it drops out of your live buckets instead of cluttering Backlog.
  - **See how long it's been sitting.** A parked loop shows how many days it's been waiting, so nothing quietly rots.
  - **Nothing gets stuck there.** Pull a waiting loop onto a day and it unparks itself back into Backlog.
  - **Press 4** on a loop to park it; `1`–`3` still move it between Pressing, Important, and Backlog.
  - **Agents can park and unpark too.** `waiting` is now a `loopBucket` value in `create_tasks` and `update_tasks`, and `get_loops` shows the Waiting section with each loop's age.
</Update>

<Update label="August 2026" description="v2.3.0" tags={["New Feature"]}>
  - **Journal.** One note a day, and nothing to set up — open Journal in the sidebar and start writing. Headings, lists and checkboxes work as you type.
  - **Your notes are files you own.** Every entry is saved as a plain `.md` file in `~/Documents/Focuh/journal`, so Claude Code, Obsidian, or any other app can read and edit them. Edit a file outside Focuh and the app picks the change up straight away.
  - **Pick your own folder.** Settings → General lets you point the journal anywhere — an Obsidian vault, a synced folder, a git repo.
  - **Ask an agent about your journal.** Two new MCP tools, `get_journal` and `write_journal`, let assistants read a day's note or add to it.
  - **Journal is ⌘5**; Settings moves to ⌘6.
</Update>

<Update label="August 2026" description="v2.2.0" tags={["Improvement"]}>
  - **One-paste MCP setup.** Preferences → MCP Server now has a **Copy setup prompt** button. Paste the prompt into any coding agent and it installs and verifies the Focuh MCP server for you — credentials already filled in.
  - **Setup for five clients.** Claude Code gets a one-line `claude mcp add` command, Cursor gets a one-click install deeplink, and Claude Desktop keeps the config-file route.
  - **Hosted remote connector.** Claude.ai and ChatGPT can now connect to Focuh at `https://www.focuh.com/api/mcp` as a custom connector with OAuth — no Node.js, no API key to paste.
  - **Raycast and Poke support removed.** Both were unmaintained and their instructions no longer worked; the docs now cover the clients we actually test.
</Update>

<Update label="July 2026" description="v0.3.0" tags={["Feature"]}>
  - **A new Focuh.** Redesigned from the ground up: a calmer, faster interface with a sidebar that gets out of your way.
  - **Today** — your day on one screen: tasks beside your calendar, with drag-to-schedule and two-way Google Calendar sync.
  - **Week** — the board, redesigned.
  - **Focusing** — a full-screen focus view with your subtasks and timer.
  - Prefer the old look? Switch back anytime in Settings → General.
</Update>

<Update label="January 2026" description="Unreleased" tags={["New Feature", "Bug Fix", "Improvement"]}>
  - **AI message persistence fix**: Fixed AI assistant replies disappearing after sending the next message. Assistant responses are now persisted to the database before the stream closes, ensuring they remain visible in the chat history.
  - **AI workflow guard**: Fixed AI assistant getting stuck repeating onboarding questions (e.g. "What could you finish THIS MONTH?"). The chat route now detects the current workflow step and injects anti-loop protection, forcing tool calls when appropriate and preventing re-asks of answered questions.
  - **Attempt caps per phase**: If the AI stays in the same onboarding phase for 3+ turns without advancing, the system escalates—extracting the best available goal content from user messages and forcing a createGoal call. Prevents infinite loops even with vague user input.
  - **Prompt editor (dev)**: The "Prompt" button in the AI chat panel now works—reads and writes the system prompt file in development mode. Prod displays a clear error since the filesystem is read-only.
  - **Instant Mission toggle**: Switching between Week and Mission views now feels instant—goals and chat data are cached in memory, AI settings are shared across components (eliminating duplicate fetches), and the chat sidebar loads in the background while goals paint immediately
  - **Snappier drag-and-drop**: Moving tasks between columns now feels instant—optimistic updates commit synchronously before the drag preview disappears, eliminating the perceptible delay on drop
  - **Multi-provider AI Assistant**: Choose between Claude (Anthropic), ChatGPT (OpenAI), or Gemini (Google) to power the AI assistant. Configure API keys for each provider in Settings → AI Assistant and switch between them anytime.
  - **Dynamic model selection**: Model dropdown now automatically shows the latest available models for your selected provider, derived from the installed AI SDK packages. No more outdated model lists.
  - **Goals/Mission page**: New hierarchical goals view with four time horizons (This year, This month, This week, Today) to help you plan from high-level objectives down to daily tasks
  - **AI Assistant**: Built-in AI chat panel on the Mission page powered by Claude. Reads your goals, tasks, and focus time to provide contextual advice. Proposes actions that require your confirmation before executing.
  - **Mission/Week toggle**: Navigate between the Goals/Mission view and the Week/Kanban view from the top navigation bar
  - **Shared navigation shell**: Mission and Week views now share a persistent layout, making navigation instant with no layout jump; both views have consistent nav height and a horizontally centered Mission/Week toggle
  - Fixed the signed-out landing view on `/` accidentally showing the in-app navbar
  - **AI Settings**: Configure your Anthropic API key and customize the AI's system prompt in Settings → AI Assistant
  - **AI Model Selector**: Choose which Claude model powers the AI assistant (Sonnet 4, Opus 4, 3.7 Sonnet, or 3.5 Haiku) from Settings → AI Assistant
  - **Chat memory**: AI conversations are persisted across sessions with full thread history
  - Fixed AI chat replies briefly appearing then disappearing in new chats (server now authenticates Convex writes using Clerk's `convex` JWT template)
  - **AI Tool-enabled Assistant**: The AI can now read your goals, tasks, calendar events, and focus sessions via tools. Ask questions like "What are my goals?" or "Show me today's tasks" and the AI will fetch live data from your account.
  - **AI Action Proposals with Confirmation**: The AI can propose changes to your goals, tasks, calendar events, and focus sessions. Each proposal appears as a card with Confirm/Cancel buttons—nothing is modified until you explicitly confirm.
  - **AI Google Calendar Sync**: The AI can propose syncing or unsyncing tasks and calendar events with Google Calendar. On confirmation, events are created/updated/deleted in your connected Google account.
  - **AI Focus Session Control**: The AI can propose starting, pausing, resuming, or ending focus sessions on your behalf (with confirmation).
  - **Expanded AI Actions**: New action types for updating tasks (title, estimate, priority), scheduling/unscheduling time blocks, moving tasks between dates, and managing internal calendar events.
  - **Clerk↔Convex auth integration**: Database functions now derive user identity from Clerk JWT tokens for improved security; user IDs are never passed from client
  - **Bounded AI context**: Thread summaries auto-compress conversation history; user memory stores stable facts/preferences; both capped to prevent token inflation
  - **AI agent auditing foundation**: All AI tool calls use internal Convex functions with ownership verification
  - **Blocking permissions banner**: Desktop app now shows a top-nav banner when website blocking can't work due to missing permissions (Accessibility or Automation)
  - Clarified that **app blocking is macOS-only** (for now) and prevented native blocking sync attempts on unsupported platforms (fixes confusing “it doesn’t work” states on Windows/web)
  - **Windows website blocking (extension)**: Added `/extension-auth` + `/api/focus-status` so the Focuh Blocker browser extension can enforce website blocking on Windows (Chrome/Edge) without macOS Accessibility
  - Added **Automation permission check** for website blocking; previously only Accessibility was checked, but AppleScript browser control also requires Automation permission in macOS
  - Permissions modal now shows Automation status alongside Accessibility and Notifications with a button to trigger browser permission prompts
  - Expanded browser prewarm to include Dia, Comet, Sigma, Atlas, and ChatGPT so permission prompts appear during setup rather than mid-session
  - Improved logging of AppleScript failures to help diagnose permission issues
  - **Blocked apps are now closed** instead of just hidden during focus sessions; if the app refuses to quit (e.g., unsaved changes dialog), it falls back to hiding; persistent re-opens escalate to force-terminate
  - **Windows download**: `/download` now surfaces Windows installers (x64) when available, and the primary download button auto-picks macOS vs Windows based on your device
  - Task cards now show **minutes left** in real time while focusing (e.g. 60 → 54 after 6 minutes)
  - Fixed "today" focus attribution across the Europe/Berlin midnight boundary (sessions that cross midnight no longer land on the wrong day)
  - Community dropdown now only shows focusing + online users (no "offline focused today" rows)
  - Clarified that "today" labels use the Europe/Berlin day boundary
  - Improved online presence accuracy across multiple tabs by relying on heartbeat TTL rather than deleting presence on unmount
  - Focus sessions no longer auto-pause after estimate + 30 minutes (prevents sessions feeling "stuck" around 2 hours for common estimates like 90 minutes)
  - Leaderboard "todos completed" no longer counts subtasks (only top-level tasks)
  - 2026 leaderboards: year grid now matches the page width; restored "Top 10 contenders" under the grid; future-day tooltip now says "To be determined"
  - Fixed blocked-site redirect showing "No active focus session" after pausing/stopping/resuming a task; now reliably shows the task name
  - Fixed Google Calendar sync showing events 1 hour ahead by using the client's timezone instead of UTC when creating/updating events
  - **Polished input focus states**: Removed the ugly inner gray outline on chat and goals inputs; standardized subtle monochrome hover/focus/disabled states across all text inputs for a cleaner, more cohesive feel
  - **Robust focus time tracking**: Focus time now flushes immediately when you pause or quit the desktop app—no more "lost" minutes. Leaderboards and community dropdown show your actual focused time without the previous caps that made overtime sessions appear stuck. Every second you focus is now accurately tracked and persisted.
</Update>

<Update label="December 2025" description="v0.2.21" tags={["Bug Fix", "Improvement"]}>
  - Fixed false "Update check failed" errors by bundling the UI into the desktop app
  - Update check failures are now silent on automatic checks; errors only shown when you manually check
  - Fixed false "older version" banner appearing on modern app versions
  - Desktop app now loads instantly (no network dependency for initial render)
  - Improved error message for users who need to manually update (links directly to download page)
</Update>

<Update label="December 2025" description="v0.2.19" tags={["New Feature", "Improvement"]}>
  - Added Long-Term Goals with time-based streaks (optional via Settings → Goals)
  - Goal ring indicator in the navbar shows daily progress and streak; hover reveals goal title
  - Focus sessions on goal-linked tasks automatically count toward the goal
  - Needle Mover tasks auto-link to the active goal; streak is visible on leaderboards
</Update>

<Update label="December 2025" description="v0.2.18" tags={["Bug Fix"]}>
  - Fixed time input on task cards: users can now enter any time value (e.g., 90 minutes) even on tasks with extensive focus history
  - Time pill now shows and edits the estimated time directly, making it simpler and more intuitive
</Update>

<Update label="December 2025" description="v0.2.17" tags={["Bug Fix"]}>
  - Fixed critical bug where focus time was lost when session was auto-paused by cleanup
  - Previously, if your laptop slept briefly during a focus session, only the time before sleep was saved
  - Now all focus time is correctly tracked and saved, even if the session is interrupted
</Update>

<Update label="December 2025" description="v0.2.16" tags={["Bug Fix"]}>
  - Fixed auto-update not working by adding missing `updater:default` permission to Tauri capabilities
  - Users on v0.2.14 or v0.2.15 will need to manually download this version, but future updates will work automatically
</Update>

<Update label="December 2025" description="v0.2.15" tags={["New Feature"]}>
  - New Focus Page: when visiting blocked websites, you're redirected to a compassionate refocus page instead of a popup overlay
  - Helps you break down tasks with "What can you get done in 5 minutes?" prompt
  - Creates subtasks directly from the focus page
  - 5-minute sprint timer with subtask list to help you get back into flow
  - Subtasks are checkable from both the main view and sprint timer
</Update>

<Update label="December 2025" description="v0.2.14" tags={["Bug Fix"]}>
  - Added NSUserNotificationUsageDescription to Info.plist for proper macOS notification permission prompts
  - Fixed app icon (32x32.png) format to RGBA for Tauri build compatibility
</Update>

<Update label="December 2025" description="v0.2.10" tags={["Improvement", "Bug Fix"]}>
  - Improved Tauri startup experience: show a native splash immediately and avoid the long blank/white window while the UI loads
  - Fixed blocked-site focus overlay window corners by making the native overlay transparent (prevents background bleed behind the rounded UI)
  - Fixed notification permission check to use native macOS API instead of unreliable Web Notification API
  - Standardized blocked-app notifications to use Tauri’s official notification plugin; in `tauri dev` macOS may group them under Terminal
  - Prevented confusing “Add” no-ops in Focus Blocker by disabling add when a website/app is already on the blocklist
  - Aligned day headers with task cards and section dividers for cleaner visual rhythm
  - Fixed a mobile UI build parsing error triggered by an invalid TSX comment placement
  - Fixed Vercel build failing on `/auth/tauri` by wrapping `useSearchParams()` behind a Suspense boundary (Next.js 16 requirement)
</Update>

<Update label="December 2025" description="v0.2.13" tags={["Bug Fix", "Improvement"]}>
  - Fixed Tauri auth onboarding: “Sign in with browser” now reliably opens the system browser (adds a native OS-level fallback when shell plugin open fails)
  - Moved the desktop app version label to the left of the focus indicator in the top bar
</Update>

<Update label="December 2024" description="v0.2.8" tags={["Bug Fix"]}>
  - Fixed app/website blocking continuing after focus session ends
  - Improved notification icon handling on macOS
  - Added cleanup to ensure blocking stops when session ends from any source
  - Request notification permission on launch (Tauri) and show real notification permission status in the permissions modal
</Update>

<Update label="December 2024" description="v0.2.0" tags={["New Feature", "Improved"]}>
  - Native macOS website blocking via Accessibility API
  - Shows blocking modal when visiting distracting sites during focus sessions
  - Modal reappears after 15 seconds if closed while still on blocked site
  - No admin password required - uses browser URL monitoring
  - Redesigned blocking modal with minimalistic Apple-style design
  - Reduced modal size from 1000x680 to 440x260 for cleaner appearance
  - Added 12px rounded corners to modal window for modern macOS appearance
  - Refined typography and spacing for better readability
  - Focus Blocker settings redesigned with Websites/Apps segment control
  - Added master toggle to enable/disable blocking (on by default for macOS)
  - Web users see "Only on MacOS" messaging with disabled inputs
</Update>
